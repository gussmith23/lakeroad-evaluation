# DSP-Annotated Benchmarks

This folder contains benchmark designs
  which utilize annotations (such as
  Xilinx's `(* use_dsp = "yes"*)` annotation)
  to indicate that modules should be mapped
  to DSPs.
